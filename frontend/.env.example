# Used when the frontend is served separately from the backend.
# Leave empty for same-origin production builds served by FastAPI.
VITE_BACKEND_ORIGIN=

# Vite dev-server proxy target.
VITE_DEV_API_TARGET=http://127.0.0.1:8000
