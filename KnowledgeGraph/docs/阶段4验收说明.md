# 阶段4验收说明（视频文本化匹配方案）

## 1. 验收结论
- 阶段4状态：`ok`
- 质量门禁：`passed = true`
- 验收时间：`2026-04-22 21:32:56`（见 `stage4_run_report.json` 时间戳）

## 2. 方案说明（替代旧图谱视频映射）
- 已不依赖 `data/course/big_data.json` 作为视频匹配依据。
- 视频匹配基于：`视频URL + 字幕/ASR转写文本 + 新图谱concept语义`。
- 流程采用：字幕优先，ASR回退；多路召回（BM25/关键词）+ 加权重排（语义/关键词/层级一致性）。

## 3. 核心验收指标
- `video_transcript_coverage = 100.0%`（目标 `>= 85%`）
- `video_match_rate = 41.59%`（目标 `>= 40%`）
- `avg_video_match_score = 0.3237`（已产出）
- `matched_concept_count = 47`，`needs_review_count = 16`，`unmatched_count = 50`

## 4. 可追溯结果文件
- 阶段运行报告：`KnowledgeGraph/data/output/stage4_run_report.json`
- 阶段摘要：`KnowledgeGraph/data/output/stage4_summary.json`
- 质量报告：`KnowledgeGraph/data/output/quality_report.json`
- 视频匹配明细：`KnowledgeGraph/data/output/video_match_report.json`
- 全量资源匹配报告：`KnowledgeGraph/data/output/resource_match_report.json`

## 5. 人工复核材料（无需直接播放m3u8）
- 已匹配复核表：`KnowledgeGraph/data/output/video_review_pack_matched/review_pack.csv`
- 待复核表：`KnowledgeGraph/data/output/video_review_pack_needs_review/review_pack.csv`
- 注：音频片段 `clips/` 体积较大，默认不纳入Git提交；如验收方要求可单独打包发送。

## 6. 复现命令
```bash
python KnowledgeGraph/scripts/run_pipeline.py --stage 4
```

复核包导出（可选）：
```bash
python KnowledgeGraph/scripts/export_video_review_pack.py --decision matched --top-n 47 --clip-seconds 12 --output-dir KnowledgeGraph/data/output/video_review_pack_matched
python KnowledgeGraph/scripts/export_video_review_pack.py --decision needs_review --top-n 16 --clip-seconds 12 --output-dir KnowledgeGraph/data/output/video_review_pack_needs_review
```

## 7. 备注（m3u8播放问题）
- 本地播放器无法播放 `m3u8` 不影响验收结论。
- 当前工程已支持 `HTTPS` 优先与 `ffmpeg` 拉流抽音频，可用证据片段+文本进行匹配准确性复核。
