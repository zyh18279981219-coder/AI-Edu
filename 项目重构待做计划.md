# AI-Education 项目重构待做计划

本文档维护 `zyh` 分支后续开发步骤。目标是把项目逐步改造成需求文档、概要设计和详细设计描述的系统形态，并在开发过程中同步维护需求文档、概要设计、详细设计、接口说明和数据库表结构。

## 0. 当前基线

- 当前开发分支：`zyh`
- 后端目录：`backend/`
- 前端目录：`frontend/`
- 数据库资料：`database/`
- 正式文档目录：`docs/`
- 后端入口：`main.py`
- 当前数据库：本地 MySQL `ai_education_design`，不再按远程数据库或 SQLite 作为开发基线。
- 当前详细设计基线：
  - `docs/detailed-design/AI-Education系统详细设计说明书-开发基线版-2026-06-28.md`
  - `docs/detailed-design/AI-Education系统详细设计说明书-开发基线版-2026-06-28.docx`

## 1. 开发规则

- 代码调整必须对齐详细设计；如果需求变更，先改设计说明，再改代码。
- 需求文档只写业务目标、角色、流程、规则和验收，不写数据库字段实现。
- 概要设计写系统架构、模块协作、数据流向、权限边界和核心接口边界。
- 详细设计写页面设计、业务规则、异常处理、接口说明、指标公式、数据库表结构和模块内部流程。
- 数据库结构先在本地 MySQL 验证，稳定后同步 `database/schema.sql` 和设计文档。
- 每轮开发完成后检查：需求文档、概要设计、详细设计、接口说明、数据库 schema、核心 smoke test。

## 2. 当前数据库状态

- 本地库：`ai_education_design`
- 当前 schema：42 张表。
- 当前数据性质：smoke/demo 验证数据，不是正式演示数据。
- 已接入基础业务链路：课程图谱、课程资源、职业能力映射、作业覆盖知识点、学生画像、测验记录、诊断报告、诊断人工修正、个性化路径节点状态、资源学习事件、教师干预任务包。
- 仍主要为空或待产品化：教师互动/教研事件、5E 有效性记录、正式演示数据。

## 3. 已完成事项

- [x] 切换到 `zyh` 分支并完成前后端目录整理。
- [x] 将 `.env` 切换到本地 MySQL。
- [x] 建立本地 MySQL 开发库 `ai_education_design`。
- [x] 删除运行时 SQLite 依赖方向，后续以 MySQL 为唯一开发数据库。
- [x] 将数据库 schema 扩展到 42 张表。
- [x] 跑通后端编译：`python -m compileall -q backend main.py`。
- [x] 跑通前端构建：`cd frontend && npm run build`。
- [x] 生成开发基线版详细设计文档。
- [x] 作业覆盖知识点配置表接入作业创建、查询和更新接口。
- [x] 个性化路径节点状态表接入路径生成。
- [x] 资源学习事件表接入记录、汇总接口，并纳入课程运行评估。
- [x] 资源学习事件进入学生诊断证据时间线，核心 smoke 已验证 `resource_learning` 证据回流。
- [x] 学生端学习诊断页和教师端干预诊断页展示资源学习、作业、测验证据时间线。
- [x] 诊断人工修正记录接入数字孪生 API。
- [x] 教师智能干预任务包接入 MySQL：草稿、修改、下发、学生接受/作答/完成、教师评分均同步落库。
- [x] 扩展核心 smoke test，覆盖课程图谱、能力映射、作业、画像、测验、资源学习事件、诊断修正、路径状态、干预任务包和课程运行评估。
- [x] 教师端课程数字孪生建设台接入：支持教师按课程大纲生成初始知识图谱、绑定资源候选、审核启用/禁用资源并发布课程底座。
- [x] 修复系统诊断中发现的前端接口路径、父路由重定向、首页文案 key、课程孪生资源刷新容错；默认课程补种增加开关，课程资源同步保留已有审核/启用/软删除状态。
- [x] 个性化学习路径学生端状态闭环：学习中心和课程内容页展示路径节点状态，支持开始学习、标记完成，并同步更新 `learning_path_node_status`。
- [x] 教师看板学生详情接入统一诊断服务：展示诊断摘要、证据等级、置信度、薄弱点原因、建议动作和证据时间线。
- [x] 学生数字孪生接入作业证据增强：章节主观题/代码题聚合为章节综合实践能力；教师确认的作业覆盖知识点作为叶子知识点辅助证据进入画像摘要和诊断统计。

## 4. 下一阶段优先级

1. 学生数字孪生增强：
   - 职业能力达成等级在学生端和教师端差异化展示。
   - 将证据时间线进一步接入教师看板学生详情页。（基础展示已接入，后续补修正入口、筛选和作业证据贡献展示）

2. 教师看板与诊断智能体对齐：
   - 教师批量诊断读取统一 `StudentDiagnosisService`。（学生详情已接入，班级批量诊断待接）
   - 教师端展示证据时间线、证据等级、置信度和可修正入口。（基础展示已接入，修正入口待接）
   - 诊断修正后在教师看板中可追踪、可查询。

3. 教师智能干预产品化：
   - 完善教师审核页面。
   - 干预任务包只包含学生端线上任务。
   - 学生完成后回流学生画像、教师看板和教师数字孪生。
   - 区分草稿、已下发、进行中、已完成、暂不执行等状态。

4. 课程数字孪生继续增强：
   - 外部资源真实自动检索、质量过滤和替换流程。
   - 本地资料上传后的转换、RAG 入库和知识点绑定状态。
   - 能力缺口补知识点流程：大模型推荐，教师决定是否新增正式知识点。
   - 课程运行评估补重复访问、路径停滞、正式测验定义等埋点。

5. 个性化学习路径继续增强：
   - 路径节点状态的学生端展示和更新。（已完成基础闭环）
   - 资源、小测、作业入口对齐正式课程图谱。
   - 缺少合适作业时只提示教师补充或生成草稿，不自动发布。
   - 图谱外大模型建议只作为“补充学习项”。

6. 教师数字孪生与 5E：
   - 教师行为事件采集。
   - 六维指标稳定计算与证据下钻。
   - 5E 有效性指标：有效互动、完成度、测验提升、路径继续学习率。
   - 5E 事件回流学生画像，但不替代测验和作业。

7. 演示与测试：
   - 准备清库脚本和稳定演示数据。
   - 拆分更细的接口 smoke test。
   - 前端页面按需求文档逐步重构。

## 5. 当前风险

- 当前 smoke 数据重复较多，后续需要正式演示数据和清库脚本。
- 需求文档比当前产品实现更完整，不能把当前代码当成最终系统形态。
- 外部资源真实检索、本地资料 RAG 入库、5E 有效性仍是较大缺口。
- 前端页面尚未完全按新需求重构，需要边开发边维护接口说明。
- 当前工作树仍处于大规模目录重构状态，提交前必须确认 `backend/`、`frontend/`、`database/`、`docs/` 新目录全部纳入版本控制，避免只提交旧目录删除。
- 核心业务主要依赖会写本地 MySQL 的 smoke 验证，后续需要拆分隔离 pytest/integration test。
