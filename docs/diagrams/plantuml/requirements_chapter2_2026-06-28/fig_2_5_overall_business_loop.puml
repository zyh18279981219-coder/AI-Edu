@startuml
!theme plain
title AI-Education 整体业务闭环流程
skinparam backgroundColor #FFFFFF
skinparam defaultFontName "Microsoft YaHei"
skinparam defaultFontSize 18
skinparam roundcorner 12
skinparam shadowing false
skinparam ArrowColor #4A5568

start
:教师/管理员建设课程底座;
:维护课程结构、资源、作业关系、岗位能力映射;
if (课程底座已审核发布?) then (是)
  :学生进入学习空间;
  :学习资源、完成测验、作业、互动和干预任务;
  :系统沉淀学习证据;
  :更新学生数字孪生画像;
  if (证据是否足够诊断?) then (足够)
    :诊断智能体解释薄弱原因;
    :个性化路径生成学习安排;
    :教师看板展示风险学生、薄弱知识点和依据;
    if (需要正式干预?) then (需要)
      :生成干预任务包草稿;
      :教师审核、修改并确认下发;
      :学生完成干预任务;
      :结果回流画像和看板;
    else (暂不需要)
      :持续学习与观察;
    endif
    :课程数字孪生评估课程健康、资源缺口和能力风险;
    if (需要维护课程底座?) then (需要)
      :教师补充资源、测验、作业或修订能力映射;
      :重新校验并发布新版本;
    else (不需要)
      :保持当前课程版本运行;
    endif
  else (不足)
    :提示补充测验、作业、学习记录或资源证据;
  endif
else (否)
  :返回课程建设与审核;
endif
stop
@enduml
